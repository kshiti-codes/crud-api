{
  "name": "books-api",
  "version": "1.0.0",
  "description": "Books API deployed on Vercel",
  "main": "api/books.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}